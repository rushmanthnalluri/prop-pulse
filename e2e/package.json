{
  "name": "proppulse-e2e",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Playwright end-to-end tests for the PropPulse dashboard (real Chromium against live backend :8200 + frontend dev server :5300).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1"
  }
}
