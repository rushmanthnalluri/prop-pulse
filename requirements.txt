# PropPulse — full development environment (ML + API + notebooks + tests).
# Python 3.14.5 (Windows), pinned to the versions verified installed in .venv.
#
# Pin rationale (see docs/DECISIONS.md ADR-6):
#   - pandas==2.3.3 (not 3.x): mlflow 3.15.1 declares Requires-Dist: pandas<3.
#   - numpy==2.4.6 (not 2.5.x): numba 0.66.0 (shap dependency) declares numpy<2.5,>=1.22.
#   - numba is pinned explicitly because it is what shap imports at runtime.

# --- core science ---
pandas==2.3.3
numpy==2.4.6
scipy==1.18.0
scikit-learn==1.9.0
matplotlib==3.11.1
seaborn==0.13.2
joblib==1.5.3

# --- modeling / explainability / tracking ---
xgboost==3.4.0
shap==0.52.0
numba==0.66.0
mlflow==3.15.1

# --- API ---
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# --- tests / notebooks / misc ---
pytest==9.1.1
httpx==0.28.1
jupyter==1.1.1
nbconvert==7.17.1
requests==2.34.2
